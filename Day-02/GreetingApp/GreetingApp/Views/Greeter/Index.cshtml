@using GreetingApp.Controllers
@model GreetingApp.Controllers.GreetInput
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <title>Index</title>
    <link href="/Content/Site.css" rel="stylesheet" />
</head>
    <body>
        <h1>Greeter</h1>
        @*  <form action="/Greeter/Greet" method="post">
            <label>Name :</label>
            <input type="text" name="FirstName" value="@Model.FirstName" />
            <input type="text" name="LastName" value="@Model.LastName" />
            <input type="submit" value="Greet" />
            <ul>
                @foreach (var item in Model.ErrorMessages)
                {
                    <li>@item.Value</li>    
                }
                
            </ul>
        </form>*@
        @*
        @using (Html.BeginForm("Greet", "Greeter"))
        {

            <label>Name :</label>
            <div>
                $1$ @Html.TextBoxFor(m => m.FirstName)    
                @Html.ValidationMessageFor(m => m.FirstName)#1#
                @Html.EditorFor(m => m.FirstName)
            </div>

            <div>
                @Html.TextBoxFor(m => m.LastName)    
                @Html.ValidationMessageFor(m => m.LastName)
            </div>
            <input type="submit" value="Greet" />
        
        }*@
        
        @using (Html.BeginForm("Greet", "Greeter"))
        {
            @Html.EditorForModel();
            <input type="submit" value="Greet"/>
        }
    </body>
</html>
